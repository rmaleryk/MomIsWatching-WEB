@using System.Web.Script.Serialization
@model IEnumerable<MomIsWatching.Models.DeviceLog>


@foreach (MomIsWatching.Models.DeviceLog deviceLog in Model)
{
    var json = new JavaScriptSerializer().Serialize(deviceLog);
    <script>
        deviceSignIn(JSON.parse(@json));
    </script>
}